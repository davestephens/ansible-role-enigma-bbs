---
enigma:
  home: "{{ ansible_env.HOME }}/enigma_bbs"
  repo: https://github.com/NuSkooler/enigma-bbs
  nvm_user: "{{ ansible_user_id }}"
  version: master

nvm:
  version: v0.33.4
  node_version: 6.11.3
  user: "{{ ansible_user_id }}"